import 'package:flutter/material.dart';
class NavigationElement extends StatelessWidget {
  const NavigationElement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
